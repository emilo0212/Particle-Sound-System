#pragma once
#include "main.h"
#include <time.h>
#include <stdbool.h>

namespace Utility {
	extern int randomIntNumber(int min, int max);
	extern double randomDoubleNumber(double min, double max);
	extern float randomFloatNumber(float min, float max);
}